# D3-A32FB_MCU 项目规则

## 语言与 Skill 文档规则

- 所有对话默认使用简体中文；用户明确要求其他语言时除外。
- 新建或修改 `SKILL.md` 及相关 skill 文档时，默认使用简体中文。若 skill 规范要求 `name` 字段使用小写英文、数字和连字符，则仅该字段使用合规英文标识，其余标题、说明和正文使用中文。

## 打印日志语言规则

- 新增、生成或修改的代码打印日志必须使用简体中文，包括 `InfoPrint`、`DebugPrint`、`ErrorPrint`、`printf`、`LOG` 宏和协议调试输出。

## C 函数注释规则

- 新建、生成或修改 C 函数时，必须使用简体中文 Doxygen 风格函数头注释，至少包含 `@brief`。
- 函数存在关键逻辑、状态切换、协议字段处理、持久化、硬件输出、异常路径或兼容性约束时，必须补充 `@note`，按 `Step1`、`Step2`、`Step3` 说明决策流程和业务原因。
- 存在参数时，使用 `@param[in]`、`@param[out]` 或 `@param[in,out]` 说明每个参数；存在返回值时，使用 `@retval` 说明业务含义。
- 复杂函数体内在关键阶段前使用 `/* Step1: ... */` 等中文注释；注释解释业务决策和副作用，不逐行翻译代码。

## Git 提交前置确认规则

- 严禁自动执行 `git commit`。用户要求编写 commit 或提交信息时，默认仅提供提交标题和正文草稿。
- 执行 `git add`、`git commit`、`git merge`、`git rebase`、`git reset`、`git tag`、`git push` 或其他改变 Git 状态、历史或远端的操作前，必须展示拟执行命令、文件范围和提交文案，并取得用户明确确认。

## Git 工作目录约定

- Git 仓库根目录为 `E:\software\git\AHTT\D3-A32FB_MCU`。
- 执行 `git status`、`git diff`、`git log`、`git show`、`git add`、`git commit` 等 Git 指令时，默认将工作目录设置为该目录，不在上级目录执行。

## Skill 路径约定

- 用户级通用 workflow skills 默认位于 `C:\Users\Administrator\.codex\skills`。
- 当前项目专用嵌入式 skills 默认位于 `E:\software\git\AHTT\.codex\skills`。
- 读取 skill 文件时，先按用户级通用与项目级嵌入式两类路径判断，不假设所有 skill 均位于项目 `.codex\skills` 目录。

## C 代码变量声明规则

- 新增或修改 C 函数时，局部变量声明统一放在函数开头的变量声明区。
- 不得在函数中间语句块、`if/else`、`switch/case`、循环体或 `for (...)` 初始化语句中新增局部变量声明。
- 变量依赖运行期上下文时，仅上移声明；赋值和函数调用保持在原有语义正确的位置，并检查作用域扩大后的未使用变量、初始化副作用和复用风险。

## 代码修改前置确认规则

- 执行代码修改前，必须先展示完整方案并取得用户明确确认。方案应包含修改目标、涉及文件、具体的新旧代码差异或伪代码说明及预期效果。
- 用户仅要求修改非代码说明文档、规则文档或纯文本笔记时，可直接执行；明确的 typo 修正或用户提供完整代码并明确要求写入时，也可直接执行。

## C数值字面量后缀规则

- 新增或修改C代码时，数值字面量不得使用`U`、`L`、`UL`、`ULL`等后缀。
- 十六进制宏写成`0xEA`，不得写成`0xEAU`。
- 十进制宏或赋值写成`12`，不得写成`12U`。
- 本规则适用于宏、枚举、赋值、比较以及方案文档中的C代码示例。
- 除非用户对具体位置另行明确允许，不得添加上述后缀。
- 不因本规则批量改写与当前任务无关的既有代码。

## C协议报文长度规则

- 后续新增或修改 AHTT 协议报文组包代码时，`dataLen` 的直接累加、赋值或与固定字段长度的运算不得使用长度宏，必须使用协议规定的原始数值，例如 `dataLen += 20;`。
- 当该固定字段长度宏定义于 AHTT 模块头文件时，应删除该宏定义，并将其在 AHTT 模块内的其他引用同步替换为相同原始数值，避免保留无效或未定义引用。
- 其他模块定义的版本号、配置项或非 AHTT 私有长度宏不因本规则删除。

## AHTT 未使用形参规则

- 在 `Protocol_AHTT` 目录中，不添加或保留 `(void)变量;` 形式的未使用形参标记。
- 未使用形参仍须保留在函数声明与定义中，以维持既有函数指针、回调和公开接口兼容；仅删除对应的 `(void)变量;` 语句。
- 不因形参未使用而删除函数形参、修改函数原型或改变调用方。
- 修改后检索整个 `Protocol_AHTT`，确认不存在 `(void)标识符;` 残留，并执行工程构建验证。

## C宏与结构体中文注释规则

- 新增或修改C宏定义时，必须在宏定义行末添加简洁、准确的中文注释，说明宏的用途；存在单位、范围或特殊语义时应一并注明。
- 新增或修改结构体、联合体时，每个成员变量都必须在声明行末添加中文注释，说明字段用途；涉及单位、字节序、有效范围或生命周期时应一并注明。
- 注释不得改变既有命名、数据布局或协议语义，不因补充注释批量改写与当前任务无关的代码。

## 新建文件作者规则

- 以后所有新创建的文件，只要文件头、元数据或模板中存在 `Author`、`author` 或“作者”字段，该字段必须统一填写 `wdy`。
- 不得在新创建文件的作者字段中填写 `codex`、模型名称或其他代理名称。
- 本规则仅约束新创建文件，不因本规则批量修改与当前任务无关的既有文件作者信息。

## C函数单出口规则

- 新增或修改C函数时，必须采用单出口结构；函数体只能在末尾保留一个统一的 `return` 语句。
- 不得在参数检查、错误处理、条件分支、循环或其他中间位置使用提前 `return` 退出函数。
- 有返回值的函数应在函数开头声明返回值变量，通过条件保护块或状态变量设置返回结果，并在函数末尾统一返回。
- `void`函数也不得通过中间位置的 `return;` 提前退出，应使用条件保护块组织控制流并自然执行到函数末尾。
- 不得为了满足单出口规则引入无必要的 `goto`、重复副作用或改变原有执行语义。
- 例如组包函数应先将返回值变量初始化为失败值，仅在输入合法的条件保护块内执行组包并更新返回值，最后在函数末尾统一返回；不得在参数非法分支中提前返回。
- 不因本规则批量改写与当前任务无关的既有函数；修改现有函数时，改动范围内的函数必须同步满足本规则。

## 防御性保护边界规则

- 不得在代码中添加过多或重复的防御性保护；防御性校验应优先放在外部输入、协议报文、配置参数和资源创建等真实边界。
- 对已由系统初始化顺序、模块生命周期、内部调用契约或模块私有状态保证有效的对象，不重复增加空指针、状态、端口、索引或资源已创建标志判断。
- 修改或新增代码时应先对照同职责既有模块的控制流；参考实现未做内部保护且当前调用契约能够保证安全时，保持同构，不自行增加保护分支、全局门控标志或包装层。
- 如确有必要新增参考实现不存在的防御性保护，必须先说明实际可达的失败来源、缺少保护的具体后果以及不能在输入或初始化边界消除的原因，并取得用户明确确认后实施。
- 用户明确要求移除的内部防御性保护不得以“更安全”为由重新加入；后续若出现真实故障证据，应重新提交修改方案。

## 执行计划复选框同步规则

- 执行包含Markdown复选框的实施计划时，每完成并验证一个步骤，必须立即将对应的`- [ ]`更新为`- [x]`，不得等到整个计划结束后再集中补记。
- 只有取得与步骤验收条件相匹配的代码、构建、测试、文档、板端或平台证据后才能勾选；仅完成部分工作、尚未验证或验证失败的步骤必须保持未勾选。
- 每次继续执行既有计划前，先核对计划复选框与当前实际状态；发现已完成但未勾选或已勾选但证据不足时，应先纠正计划状态再继续。
- 汇报计划进度时，必须明确区分已勾选步骤、未完成步骤以及需要板端或外部平台验证的步骤。
