# AHTT Approved Plans

这里只保存新的、已批准的 AHTT AI Implementation Plan。

文件命名：

```text
YYYY-MM-DD-<task-slug>.md
```

每个 Plan 必须包含 `Approval Handoff`，记录批准状态、Planner 环境、仓库、分支、HEAD、用户批准声明和交接时间。历史 Plan 保留在原目录，仅作为技术与证据参考；新任务不再默认写入 `docs/superpowers/plans/`。
